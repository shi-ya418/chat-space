.Side-bar
  .Side-bar__user-name
    .Side-bar__user-name__items
      .Side-bar__user-name__name 
        = current_user.name
      .Side-bar__user-name__icon 
        = link_to new_group_path do
          = icon('fas', 'edit')
        = link_to "" do
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog')
  .Side-bar__group-list
    - current_user.groups.each do |group|
      .group
        = link_to "edit_group" do
          .group__name
            = group.name
          .group__message 
            まだメッセージはありません
 
